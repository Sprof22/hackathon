"use client";
import { useEffect, useState } from "react";
import { api } from "../lib/api";
type Alert={id:string;kind:string;payload:{task?:string;owner?:string;evidence?:string;confidence?:number};createdAt:string};
export default function QA(){const [alerts,setAlerts]=useState<Alert[]>([]);const [message,setMessage]=useState("Loading audit trail…");useEffect(()=>{api<Alert[]>("/qa-notifications").then(v=>{setAlerts(v);setMessage(v.length?"":"No QA alerts yet.")}).catch(e=>setMessage(e.message))},[]);return <main className="flow-page"><div className="flow-wrap"><nav className="flow-nav"><a className="flow-brand" href="/">LoopClose</a><a href="/">← Dashboard</a></nav><div className="flow-heading"><h1>QA review</h1><p>Every autonomous close and uncertain outcome stays visible.</p></div><section className="list-card">{alerts.map(a=><article className="list-entry" key={a.id}><div><h3>{a.kind.replaceAll("_"," ").toUpperCase()} · {a.payload.task}</h3><p>Owner: {a.payload.owner??"Unassigned"}{a.payload.confidence?` · ${Math.round(a.payload.confidence*100)}% confidence`:""}</p><p>{a.payload.evidence?`Evidence: “${a.payload.evidence}”`:"No completion evidence was found."}</p></div><span className={`pill ${a.kind.includes("stale")?"stale":"review"}`}>{new Date(a.createdAt).toLocaleDateString()}</span></article>)}{message&&<div className="empty-state">{message}</div>}</section></div></main>}
