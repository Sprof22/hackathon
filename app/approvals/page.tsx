"use client";
import { useEffect, useState } from "react";
import { api } from "../lib/api";
type Reminder={id:string;subject:string;recipientEmail:string;emailBody:string};
export default function Approvals(){const [items,setItems]=useState<Reminder[]>([]);const [message,setMessage]=useState("Loading approvals…");async function load(){try{const data=await api<Reminder[]>("/approvals");setItems(data);setMessage(data.length?"":"No reminders are waiting for approval.");}catch(err){setMessage(err instanceof Error?err.message:"Could not load approvals");}}useEffect(()=>{load()},[]);async function approve(id:string){await api(`/approvals/${id}/approve`,{method:"POST"});setItems(items.filter(v=>v.id!==id));setMessage("Approved. The email was sent or captured according to your email settings.");}
return <main className="flow-page"><div className="flow-wrap"><nav className="flow-nav"><a className="flow-brand" href="/">LoopClose</a><a href="/">← Dashboard</a></nav><div className="flow-heading"><h1>Reminder approvals</h1><p>Nothing leaves LoopClose until a manager reviews it.</p></div><section className="list-card">{items.map(item=><article className="list-entry" key={item.id}><div><h3>{item.subject}</h3><p>To: {item.recipientEmail}</p><p>{item.emailBody}</p></div><button onClick={()=>approve(item.id)}>Approve & send</button></article>)}{message&&<div className="empty-state">{message}</div>}</section></div></main>}
